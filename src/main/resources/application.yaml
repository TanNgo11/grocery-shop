server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost :3306/grocery-shop
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
